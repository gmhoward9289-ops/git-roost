# Changelog

All notable changes to `git-roost` are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

Entries from the next release on are generated by release-please from
Conventional Commit subjects, so the PR title *is* the changelog line. The 0.1
section below was written by hand, before that was true.

## [0.4.1](https://github.com/gmhoward9289-ops/git-roost/compare/v0.4.0...v0.4.1) (2026-08-20)


### Fixed

* stop watch-mode flash while trees scan in. ([#35](https://github.com/gmhoward9289-ops/git-roost/issues/35)) ([f2e97a1](https://github.com/gmhoward9289-ops/git-roost/commit/f2e97a1dda0fe23ab387b6ff1b6d219ad2583953))

## [0.4.0](https://github.com/gmhoward9289-ops/git-roost/compare/v0.3.1...v0.4.0) (2026-08-20)


### Added

* scan well-known checkout folders and clip watch mode to the terminal ([#33](https://github.com/gmhoward9289-ops/git-roost/issues/33)) ([2b8b9b8](https://github.com/gmhoward9289-ops/git-roost/commit/2b8b9b88817a90483a37e68a15e48ed04a3d43c3))

## [0.3.1](https://github.com/gmhoward9289-ops/git-roost/compare/v0.3.0...v0.3.1) (2026-08-19)


### Fixed

* compile a real exe for the winget zip ([#31](https://github.com/gmhoward9289-ops/git-roost/issues/31)) ([2d4803b](https://github.com/gmhoward9289-ops/git-roost/commit/2d4803b9b919789eb09a62c91d09ae15a5ca9a44))

## [0.3.0](https://github.com/gmhoward9289-ops/git-roost/compare/v0.2.0...v0.3.0) (2026-08-19)


### Added

* add --repo/--filter scoping and a --check exit-code mode for hooks ([#21](https://github.com/gmhoward9289-ops/git-roost/issues/21)) ([227baab](https://github.com/gmhoward9289-ops/git-roost/commit/227baab10bb6003a379e3dd07e446e5cb8f89735))
* publish to winget ([#28](https://github.com/gmhoward9289-ops/git-roost/issues/28)) ([7ab1b56](https://github.com/gmhoward9289-ops/git-roost/commit/7ab1b56bfe12795a2b43075b37ac46f0530632f1))
* watch-mode cursor/detail view, frame change highlighting, opt-in --github PR/CI pane, --sort/--filter/--fail-on flags ([#22](https://github.com/gmhoward9289-ops/git-roost/issues/22)) ([930c693](https://github.com/gmhoward9289-ops/git-roost/commit/930c6935a3517ebcb2771051731823123f95fe13))


### Fixed

* **publish-doctor:** anchor the sha256/url sed extraction to the assignment line ([#26](https://github.com/gmhoward9289-ops/git-roost/issues/26)) ([e64f934](https://github.com/gmhoward9289-ops/git-roost/commit/e64f9340bee512d4aef509bee8bef376a8cf6cfc))


### Documentation

* add Discussions badge and pointer to README ([#27](https://github.com/gmhoward9289-ops/git-roost/issues/27)) ([12b6e60](https://github.com/gmhoward9289-ops/git-roost/commit/12b6e60969476f5054c952974178714267ca3704))

## [0.2.0](https://github.com/gmhoward9289-ops/git-roost/compare/v0.1.0...v0.2.0) (2026-08-19)


### Added

* align git-roost with the roost TUI family ([#15](https://github.com/gmhoward9289-ops/git-roost/issues/15)) ([50df715](https://github.com/gmhoward9289-ops/git-roost/commit/50df715072f25f8ca1c0cef07f3359a195dabf91))


### Fixed

* **test:** correct 'repositor' typo to 'repository' in formula test ([50f950c](https://github.com/gmhoward9289-ops/git-roost/commit/50f950cc006d396d75ba7ca2d102ea432efe4dca))


### Documentation

* **ci:** say why the tap-job seds must stay anchored and non-greedy ([#16](https://github.com/gmhoward9289-ops/git-roost/issues/16)) ([2b9f8bb](https://github.com/gmhoward9289-ops/git-roost/commit/2b9f8bbc63b0aa2987f106fa147270ea0c15a69d))

## [Unreleased]

### Added

- Homebrew formula for v0.1, in
  [gmhoward9289-ops/tap](https://github.com/gmhoward9289-ops/homebrew-tap).
- **`--repo NAME`** and **`--filter {all,dirty,stuck}`** put the watch-mode `f`
  key's view on the command line, so a one-shot render or `--json` can be
  scoped to one repo and one view without a terminal.
- **`--check`** — a scriptable gate, not a table. Exits `1` if any tree is
  `MID-OPERATION`, `DIVERGED`, or has uncommitted work, `0` otherwise, printing
  (or `--json`-ing) only the offending trees. For hooks that need to know
  whether a tree is safe to start work in before an agent touches it, rather
  than a person reading a screen.

## [0.1] - 2026-07-31

Initial release.

### Added

- **`top` for git** — every repo and worktree on the machine in one table, most
  actionable first. Single file, stdlib only, Python 3.9+, macOS/Linux/Windows.
  Where [roost](https://github.com/gmhoward9289-ops/roost) answers *what are my
  Claude sessions doing*, `git-roost` answers *what is actually in the trees they
  are working in* — sessions report intent, git reports what happened, and only
  one of the two can be wrong.
- **Groups ordered by the cost of ignoring them**, not by size. `MID-OPERATION`
  comes first because a tree halted mid-rebase is indistinguishable from an idle
  one in every other column.
- **Drift compares against the branch upstream, falling back to `origin/HEAD`**
  when there is none. The fallback is load-bearing rather than cosmetic:
  `ccwork` branches are never pushed, so an upstream-only comparison reports
  every one of them as clean.
- **`--json` records** for downstream consumers, with staged and unstaged counts
  reported separately rather than summed. `parse_porcelain_v2` already sees the
  `XY` pair, and collapsing it discarded the index/worktree distinction that
  consumers render as `+N~M`. A partially staged file counts on both sides,
  because both are true.
- **Packaging and CI** — `pyproject` (hatchling), `.deb`, and a man page, with
  the version single-sourced from `__version__`. The distribution name is the
  bare `git-roost`, so unlike `roost` — which had to publish as `roost-top` —
  the dist, command, module and package names all agree.

### Security

Both of the following were guarantees the tool advertised and did not actually
hold. They were found by review before any tagged release, and are recorded here
because `--json` output and the read-only guarantee are the two things a
consumer is entitled to rely on.

- **Read-only enforcement is keyed on the full argument shape, not the
  subcommand.** Gating on the subcommand alone let three mutating forms through:
  `stash list` reports but `stash pop` mutates the working tree and
  `stash clear` destroys data; `config --get` reads but `config <key> <value>`
  writes `.git/config`; and `symbolic-ref --short REF` reads but
  `symbolic-ref HEAD REF` rewrites `HEAD`. `check_read_only()` is now split out
  so tests assert the policy directly, rather than inferring it from side
  effects they hope did not happen.
- **Positional arguments are capped**, closing a hole in the fix above: a
  leading safe flag laundered a write, because `symbolic-ref --short HEAD
  refs/heads/other` opens with an accepted `--short` and then rewrites `HEAD`.
  It leaves no working-tree or index change at all, so nothing downstream would
  have noticed. The read form takes one positional, the write form takes two,
  and the count is now enforced.
- **`--json` record keys are pinned** with an exact-set assertion. Adding a key
  is safe; a rename or removal would leave a subset check green while breaking
  consumers at runtime.

[Unreleased]: https://github.com/gmhoward9289-ops/git-roost/compare/v0.1...HEAD
[0.1]: https://github.com/gmhoward9289-ops/git-roost/releases/tag/v0.1
